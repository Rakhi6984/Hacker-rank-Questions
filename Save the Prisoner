#include<stdio.h>
int main()
{
    int t;
    scanf("%d",&t);
    while(t--)
    {
        long long n,m,s;
        scanf("%lld%lld%lld",&n,&m,&s);
        long long ans=((s+m-2)%n)+1;
        printf("%lld\n",ans);
    }
    return 0;
}
