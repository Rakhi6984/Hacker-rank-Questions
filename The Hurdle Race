#include<stdio.h>
int main()
{
    int n,k;
    scanf("%d %d",&n,&k);
    int max=0;
    int h;
    for(int i=0;i<n;i++)
    {
        scanf("%d",&h);
        if(h>max)
        max=h;
    }
    printf("%d",(max>k)?(max-k):0);
    return 0;
}
