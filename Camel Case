#include<stdio.h>
#include<string.h>
#include<ctype.h>
int main()
{
    char s[100001];
    scanf("%s",s);
    int count=1;
    for(int i=0;s[i]!='\0';i++)
    {
        if(isupper(s[i]))
        {
            count++;
        }
    }
    printf("%d\n",count);
    return 0;
}
