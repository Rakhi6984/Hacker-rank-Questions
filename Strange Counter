#include<stdio.h>
int main()
{
    long long t;
    scanf("%lld",&t);
    long long cycle=3;
    while(t>cycle)
    {
        t-=cycle;
        cycle*=2;
        }
        printf("%lld\n",cycle-t+1);
        return 0;
}
